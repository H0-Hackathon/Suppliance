# =====================================================
# CoastGuard — Supply Chain Monitor
# Backend Dependencies
# =====================================================

# --- Core Web Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
starlette>=0.41.0
sse-starlette>=3.0.0
anyio>=4.0.0
python-multipart>=0.0.21

# --- Data Validation ---
pydantic==2.11.10
pydantic-settings==2.10.1
pydantic_core==2.33.2

# --- Database (SQLite for Phase 1; Aurora PostgreSQL + pgvector for Phase 3) ---
# SQLite works out of the box — no extra driver needed.
SQLAlchemy>=2.0.36
alembic>=1.13.0

# PostgreSQL / Aurora + IAM auth
psycopg2-binary>=2.9.9
pgvector>=0.3.0
boto3>=1.34.0

# --- Environment & Utilities ---
python-dotenv>=1.0.0
numpy>=1.26.0

# --- WebSockets (for demo route) ---
websockets>=16.0

# --- HTTP Client ---
httpx>=0.28.0
requests>=2.32.0

# --- Platform-specific async event loop ---
uvloop>=0.22.0; platform_system != "Windows"

# --- AI / Agent Pipeline ---
# Powers the multi-agent monitor, import-compliance, and trade-document
# pipelines (Gemini calls are made through CrewAI's built-in LLM wrapper).
crewai>=1.14.0

# --- Auth (Clerk JWT verification) ---
PyJWT>=2.9.0
passlib[bcrypt]>=1.7.4
email-validator>=2.2.0

# --- Billing ---
stripe>=11.0.0

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
